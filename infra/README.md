# `git.mari.zip` infrastructure

This folder contains Terraform files and Ansible playbooks for managing the `git.mari.zip` GitArena instance and the required stack.
Use it as reference for deploying your GitArena on your own infrastructure.
